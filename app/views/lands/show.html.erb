  <div class="main-container">
    <div class="review-container">
         <div class="card-show">
           <div class="card-body" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path @land.photo, crop: :fill%>');"">
            <div class="card-category"> </div>
            <div class="card-description"><h2><%= @land.title %></h2></div>
            <%= cl_image_tag @land.user.photo_url, class: "card-user" %>
          </div>
          <div class="card-footer">
           <p><%= @land.address%> </p>
           <p><%= @land.description %> </p>
           <p><%= @land.price %>€/night</p>
         </div>
         <!-- <a class="card-link" href="#" ></a> -->
       </div>
      <div class="review">
         <% if @land.reviews.blank? %> Be the first to leave a review
       <% else %>
          <ul>
            <% @land.reviews.each do |review| %>
            <li><%= "'"%>  <%= review.content%>   <%="'"%> <%= "🌲" * review.rating%></li>
            <% end %>
            <% end %>
          </ul>
      </div>
    </div>
    <div class="mapandbtn">

       <div
       id="map"
       style="width: 100%;
       height: 60vh;"
       data-markers="<%= @markers.to_json %>"
       ></div>
       <div class="bouton-show">
          <%= link_to "Back to the list", lands_path ,class: "btn btn-medium" %>
          <%= link_to "Edit the land", edit_land_path, class: "btn btn-medium"%>
          <%= link_to 'Delete',land_path(@land),
          method: :delete,
          data: { confirm: 'Are you sure?' },class: "btn btn-medium" %>

          <%= link_to "Book this land", new_land_booking_path(@land), class: "btn btn-treehouse" %>
        </div>
      </div>
    </div>
   </div>


