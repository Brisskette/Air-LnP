<html>
<head>

</head>
<body>
  <div class=container>
    <div class=row>
      <div class="text-center">
        <h2>Booking confirmed !</h2>
        <p>Here is the detail of your booking :</p>
      </div>
    </div>

    <div class=row>
      <div class="col-xs-12 col-sm-offset-3 col-sm-6">
        <div class="card card-booking">
          <ul>
            <li><strong>Start Date :</strong> <%= @booking.start_date %></li>
            <br>
            <li><strong>End Date :</strong> <%= @booking.end_date %></li>
            <br>
            <li><strong>Price per night :</strong> <%= @booking.land.price %> €</li>
            <br>
            <li><strong>Total Price :</strong> <%= (@booking.land.price * (@booking.end_date - @booking.start_date)).to_i %> €</li>
            <br>
            <li><strong>Landlord :</strong> <%= @booking.user.email %></li>
            <br>
            <li><strong>Address :</strong> <%= @booking.land.address %></li>
          </ul>
        </div>
      </div>
      <br>
      <div class=row>
        <div class="col-xs-12 col-sm-offset-3 col-sm-6">
          <div class="text-center">
            <%= link_to "Back to the list", lands_path, class:"btn btn-medium" %>
            <%= link_to "Add a Review", new_booking_review_path(@booking), class:"btn btn-treehouse" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>











