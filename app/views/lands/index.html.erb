<p> This is the list of all lands</p>

<ul>
  <% @lands.each do |land| %>
  <li><%= link_to land.description, land_path(land) %> Created by <%= land.user.email %></li>
  <% end %>
</ul>


<%= link_to "Add a new land", new_land_path %>
