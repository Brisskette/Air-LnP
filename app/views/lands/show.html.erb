<h2><%=@land.title %> :</h2>
<p><%=@land.description %> :</p>


<ul>
  <li>Surface : <%= @land.surface %> square meters</li>
  <li>Price : <%= @land.price %> € per night</li>

</ul>

<ul>
  <% @land.reviews.each do |review| %>
    <li> <%= review.content%></li>
  <% end %>

</ul>



<%= link_to "Make a reservation for this land", new_land_booking_path(@land) %>
<%= link_to "Edit the land", edit_land_path %>
<%= link_to "Back to the list", lands_path %>
